# 失败站点定点重抓

在本项目目录执行（期数按当天替换）：

```powershell
python -u scripts/retry_failed_sites.py 251期
```

- 只读取对应期失败TXT，按站名、URL、方向唯一匹配正式配置，逐站抓取；绝不转为全站运行。
- 成功：追加到该期成功TXT排行榜上方，更新该站该期缓存，删除对应失败记录。
- 仍失败：保留原失败记录，缓存不变。
- 原成功内容、其他站点、其他期缓存和其他TXT不变；统一排行榜及失败统计随本次成功更新。
- 缓存无效、期数不在缓存窗口、已有成功值冲突时停止写入，不重建缓存。
