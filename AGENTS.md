# 杀一头_修复版项目专属 AGENTS.md

## 1. 项目范围

适用目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀一头_修复版`

本项目只处理“杀一头”头值数据，不处理其它数据系列、生肖、波色、尾数或号码字段。当前配置共有 `238` 个站点，其中顶部 `143` 个、尾部 `95` 个。

## 2. 数据契约

- 每个站点每期最多写入一个标准化头值。
- 合法结果只能是 `0头`、`1头`、`2头`、`3头`、`4头`。
- `111头`、`一头`、`０头` 等同值必须标准化为 `1头`、`0头`。
- 头值不能是多个不同数字，也不能是 `5头` 或其它数字。
- 四头字段页面必须提供 `0` 至 `4` 中四个不同头，程序计算缺少的一个头作为唯一结果。
- 四头组合少于四个、重复、越界、边界不明或字段不唯一时必须失败。
- 用户指定期数只允许匹配该期，不能用上一期、下一期或缓存值代替。

当前配置中的字段分为：

- 一头类：`禁一头`、`绝杀一头`、`稳杀一头`、`砍杀一头`、`精杀一头`、`一头`、`杀一头`、`绝杀①头`、`必杀一头`、`大杀一头`、`期期杀①头`、`稳杀(1)头`、`禁头`、`特码输尽光`、`白姐輸盡光`、`输尽光玄机`、`猜特输尽光`、`第四剑`、`必中一头`、`稳杀`、`杀头`。
- 四头类：`④头中特`、`中四头`、`中特四头`、`特码(4)头`、`特码四头`、`四头中特`、`必中四头`、`精英四头`、`精准四头`、`公式四头`。

## 3. 配置与身份

`sites.json` 是数组。每条配置的有效字段为：

| 字段 | 项目含义 |
|---|---|
| `url` | 原始页面 URL，必须是 HTTP/HTTPS |
| `position` | 当前配置使用 `顶部` 或 `尾部` |
| `section` | 站点名称和主要身份锚点 |
| `field` | 目标业务字段 |
| `content_hint` | 栏目、作者或站名锚点；为空时程序退回使用 `section` |
| `parse_hint` | 已注册的专属解析器名称 |
| `api_url` | 可选动态文章 API；当前配置没有使用 |
| `tls_compat` | 可选兼容字段；当前配置没有使用 |

当前有效业务身份由以下字段组成：

`section + canonical_url + 规范化position + parse_hint`

`field` 不在当前身份元组中，但会进入配置指纹。`code` 只是 JSON 中的遗留编号，当前加载器、身份判断和配置指纹均不使用它；238 条配置中有 50 条没有 `code`，不得用 `code` 判重或定位站点。

当前 JSON 没有独立的 `topic`、`region` 字段。URL 中的 topic/article ID 也没有进入业务身份判定。

以下 14 条配置没有 `content_hint`，当前程序使用 `section` 作为锚点，不能据此认定已完成专属锚点验证：

`谢天谢地`、`天高云淡`、`维鸠居之`、`义无反顾`、`关怀备至`、`关云长`、`骄横跋扈`、`名列前茅`、`一衣带水`、`德高望重`、`三阳开泰`、`奋起直追`、`破甑生尘`、`段干益浮`。

## 4. 方向窗口与解析

方向名称等价关系：

- 顶部：`top`、`顶部`、`上`
- 尾部：`bottom`、`尾部`、`底部`、`下`

### 方向窗口统一硬规则

1. 每个目录必须使用自身正式配置中的方向，运行时禁止猜测、互换或临时改变方向。
2. 所有合法 `top`/`bottom` 配置的方向窗口固定为 `1` 条，不区分 `parse_hint`、页面结构或运行模式。
3. `top`、`顶部`、`上`只允许目标数据区块中的第一条有效“期数+数据”。指定期数前面不能存在任何同区块有效“期数+数据”。
4. `bottom`、`尾部`、`底部`、`下`只允许目标数据区块中的最后一条有效“期数+数据”。指定期数后面不能存在任何同区块有效“期数+数据”。
5. 边界第一条或最后一条的期数与用户指定期数不一致时，该目录立即失败；即使区块内部、页面其他位置或其它来源存在指定期数，也禁止补抓成功。
6. 禁止删除、跳过或降级边界外的有效候选后重新排序；禁止继续向区块内部寻找指定期数；禁止使用其它文档、其它区块、相邻期或缓存值补位。
7. 只有方向边界首先命中指定期数后，才能收集该期的全部高可信候选，继续检查同期值冲突、同值不同原始边界、跨区块冲突、跨来源冲突、数量、格式和合法值。
8. 方向边界只对已经证明属于同一目标文档、同一栏目和同一数据区块的有效候选生效；无效候选可以保留诊断证据，但不得占用方向窗口。
9. 本规则同时适用于单期、多期、失败验证、基线、缓存和正式判重入口，任何专属解析器都不能放宽或绕过窗口 `1`。

`is_strict_position_three()` 只是保留给旧调用方的兼容函数名，当前语义仍是窗口 `1`。`top_primary_ten_periods` 即使在解析器内部预取十条，统一选择层仍只允许第一条；`fengmaolinjiao_top_kill_head` 仍只保留顶部第一条及其同期冲突证据。

同一来源文档中的期号流如果出现相邻且完整的年度回卷 `001期 -> 365期` 或 `365期 -> 001期`，必须先按该边界切分年度周期：顶部只保留第一个周期，尾部只保留最后一个周期，再执行第一条/最后一条和同期冲突检查。`001期 -> 364期` 等非完整边界不得切分；同一选中周期内的重复期数以及不同来源文档之间的同期冲突仍必须失败。候选的 `original_position` 必须保留整份来源文档中的原始位置，不能因周期切分重新编号。

当前解析器分布：

| `parse_hint` | 数量 | 实际用途 |
|---|---:|---|
| `full_period_list` | 52 | 按字段锚点解析完整期号列表 |
| `period_bracket_head` | 42 | 期号块和字段后的括号头值 |
| `missing_head_from_four_combo` | 31 | 四头组合缺一 |
| `missing_head_from_four_combo_full_period_list` | 26 | 完整列表中的四头组合缺一 |
| `manager_anchored_missing_head_from_four_combo` | 16 | manager 动态文章四头缺一 |
| `user_kill_head` | 12 | 用户文章聚合接口 |
| `table_column` | 11 | 表格列或纵向表格 |
| `admin_anchored_missing_head_from_four_combo` | 9 | admin 动态文章四头缺一 |
| `strict_position_three` | 8 | 遗留解析器名称；选择层仍执行窗口 1 |
| `head_tail_phrase` | 5 | “头/尾”短语 |
| `anchored_missing_head_from_four_combo` | 4 | 站名锚定四头缺一 |
| `bracket_field_head` | 2 | `【字段】【头】` |
| `strict_position_three_missing_head_from_four_combo` | 2 | 遗留解析器名称；四头缺一后仍执行窗口 1 |
| 其它专属解析器 | 22 | 仅允许按注册分支解析 |

四头缺一必须严格使用 `extract_four_combo_values()` 及对应专属解析器，不能用普通 `头` 关键词兜底。

## 5. 明确站点特例

- `智能`：`https://jjlachsn.4fk3e-i2rpf-vgknlh.work:16677/`，使用 `AI斩头`。
- `天下彩`：`https://eqyyanu.4rfyf-h2bx5-oxsbom.xyz:16677/`，只认 `【杀一头】【X头】` 格式。
- `凤毛麟角`：`https://bqipgah.pvib6-ptrqe-ihcefh.xyz:16677/topic/268615.html`，只允许顶部专属第一条。
- `肆言如狂`：`https://ryfbdty.p4a53-f1hew-diwcrg.xyz:16677/topic/244751.html`，只取顶部主列表前十期。
- `不如离去`：`https://mxiscni.nkh6s-vfni4-lwgfvw.xyz:16677/topic/257907.html`，先从标题链接按期数、站名和字段进入详情页。
- `精神抖擞`：`https://xeyzmutd.ulazy-l7v28-mgqrqs.xyz:16677/topic/470790.html`，使用专属行格式。
- `赤兔`：`https://zwpwuwq.fxnb4-uer1b-uftjcw.work:16677/#am`，使用专属“杀头”短语。
- `小喵酱@`：`https://pxeeptca.oc0g1-jyj04-onsfnv.xyz:16677/topic/539434.html`，只认 `【绝杀一头】【杀X头】`。
- `恪守成式`：`https://xoxtpupt.bvptr-i3mv8-pbjxin.work:17455/topic/218073.html`，解析横线包围的头值。
- `财富高手论坛`：`https://x1rvueyk50.669332.shop/bbs/`，只认 `.jszq` 面板标题 `澳门•财富高手论坛【绝杀专区】`，并要求表头同时包含 `期数`、`杀一肖`、`杀一尾`、`杀一头`、`开奖结果`。
- `不吐不快-杀一头`：`https://993345.com/gsb1.aspx?id=2266`，使用无外层栏目括号格式。
- `国色天香`：`https://993345.com/gsb1.aspx?id=2509`，使用 `【必杀一头】【杀X头】` 格式。
- `神童资讯` 与 `老五主` 共用 `https://77.969353m.com:1888/`，依靠不同 `content_hint` 和四头锚定解析区分，禁止合并。
- `七剑下天山`：`https://kcyeko.bzx7a-tl1us-cfcaei.xyz:16677/`，使用 `sword_head`。
- `竞星剑`：`https://ykeejph.z9koz-18xjn-pvglgy.xyz:16677/`，只从站名标记后解析四头组合。
- `链路聚合`：`https://bbuueerm.pxcma-jyfok-mvduyw.xyz:16633/topic/455099.html`，顶部第一条加四头缺一。
- `宁波商人`：`https://xmdrbud.eqpr2-6tpvi-pjqztv.xyz:16677/topic/503025.html`，顶部第一条加四头缺一。
- `失心人`、`蝶恋花`、`风之语`：共用 `https://xmdrbud.eqpr2-6tpvi-pjqztv.xyz:16677/topic/257927.html`，使用 `period_title_detail_first_three_pages`。按运行时指定期数、各自 `field` 和站名锚点同时匹配标题；列表每页固定 20 条，只允许前 3 页（前 60 条）内的唯一匹配。进入详情后仍执行顶部窗口 1；第 4 页以后、标题不完整、重复详情或详情请求失败均不得成功。
- `大家发`：用户于 `2026-08-06` 明确批准“特例，只要能正常抓取到数据”。正式身份为 `https://ssytmks.kcyub-abvtn-uvalbe.xyz:16677/topic/732181.html`、顶部、字段 `绝杀一头`、文档锚点 `大家發`、解析器 `full_period_list`；217期真实顶部结果为 `3头`。本次特例只放宽近10期历史和正式判重准入门槛，不放宽期数、顶部窗口1、字段、同文档锚点、来源边界、合法值和同期冲突规则。

`strict_position_three` 当前配置站点为：`震撼六和界`、`呼风唤雨`、`杀码榜`、`信封`、`瞽旷之耳`、`千变万化`、`聪明绝顶`、`顺其自然`。这些站点也必须在顶部第一条或底部最后一条命中指定期数。

### 动态文章站点

当前共有 50 个动态文章 URL：

- `/article/admin/`：32 个；
- `/article/manager/`：18 个；
- `/article/lottery/`：0 个。

当前配置没有 `api_url`，程序按文章类型和 ID 生成：

- admin：`/api/proxy/admin-articles/{id}`
- manager：`/api/proxy/manager-articles/{id}`
- lottery：`/api/proxy/lottery-articles/{id}`

动态文章必须校验 URL 中的文章 ID、同源响应、标题、作者、正文、站名锚点、字段和指定期数。动态四头站点包括 `随风细雨`、`澳彩内幕`、`手疾眼快`、`只见远方`、`一举两得`、`山高水长`、`义形于色`、`点点灯光`、`桀骜不驯`、`酒精泡芙`、`港陆精英`、`赛马会料`、`耿耿于怀`、`百無禁忌` 等。

### 已封存站点

`我见犹怜`、`无可取代`、`受宠若惊`、`福星抱抱`、`惠泽咯`、`澳门挂椅`、`助民辉煌`只保存在 `archived_sites.json`，禁止进入活跃加载器、单期、多期、验证、缓存重建和正式判重入口；恢复必须由用户明确授权。

### 用户聚合站点

当前 12 个 `#/users/{id}` 站点使用 `user_kill_head`：

`喜欢药`、`常态胡荽`、`易逝婴儿`、`华丽手相`、`五彩缤纷`、`刚猛面食`、`亲切典礼`、`优秀服饰`、`鼓舞泥堆`、`害怕国王`、`不幸月会`、`主要针`。

程序必须先校验用户 ID 和昵称，再读取 `/api/v1/users/{id}/forums`，要求用户 ID、栏目、期数和站名锚点一致；不能使用用户聚合页中的任意第一条记录。

## 6. 入口与输出

### 单期抓取

入口：`scripts/lottery_head_scraper.py`

BAT：`抓指定期数.bat`

参数：

- 必填期数，接受 `201`、`201期` 或 `201qi`；
- 直接脚本还支持 `--dry-run` 和 `--workers 1..16`；
- BAT 实际调用 `py -3.10 scripts\lottery_head_scraper.py "%PERIOD%"`。

单期成功文件：

`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳\{期数}期-头.txt`

单期失败文件：

`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳失败\{期数}期失败-一头.txt`

调试文件：

`.tmp\lottery_head_records.json`

`.tmp\lottery_head_records.txt`

单期流程的裁决只使用本次实时抓取结果：

- 抓取前、抓取中和成功/失败判定时不得读取或使用 `.tmp\recent_10_cache.json`；
- 不得用旧缓存补成功、拒绝历史期、改写实时头值、制造缓存冲突失败或改变方向/字段/同期冲突结论；
- 成功/失败 TXT 和调试记录先按实时结果确定并写入；
- 结果确定后，单期流程只有在成功目录数 ÷ 当前活跃配置目录总数严格大于 `85%` 时才允许滚动更新 `.tmp\recent_10_cache.json`；成功率等于或低于 `85%` 时完全不触碰旧缓存；
- 达到阈值并写缓存时，失败目录必须从本期 `values`、`positions`、`provenance` 中移除，并在 `missing` 中写入失败状态和具体原因；缓存写入失败只报告缓存错误，不得回滚或改变已经确定的实时结果；
- `--dry-run` 不写 TXT，也不写缓存。

`.tmp\recent_10_cache.json` 是新增站点重复检测的独立证据，不是日常单期抓取的裁决输入。缓存清空后，下次单期成功可从当前期重新建立缓存；这不等同于完整近十期基线，正式新增判重仍须按第8节门禁执行。

### 多期抓取

入口：`scripts/lottery_head_multi_period.py`

BAT：`抓多期不刷新缓存.bat`

参数示例：`py -3.10 scripts\lottery_head_multi_period.py 187 188 189`

实际行为：

- 期数去重；
- 每个站点按给定期数顺序抓取；
- 任意一期成功即停止该站点后续期数；
- 全部期数失败才进入失败汇总；
- 不刷新 `recent_10_cache.json`。

输出：

`.tmp\multi_period_any_{期1}_{期2}_....json`

`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳\{期1}_{期2}_...多期抓取成功-一头.txt`

`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳失败\{期1}_{期2}_...多期汇总失败-一头.txt`

`scripts\summarize_multi_period_failures.py` 读取的是 `.tmp\multi_period_records_{期}.json`，而当前多期入口写入的是 `multi_period_any_*.json`，两者不能混用。

### 失败站点验证

入口：`scripts/verify_failed_sites.py`

参数：

- `py -3 scripts\verify_failed_sites.py 192期 --site "站点名称"`
- `--site` 可以重复；
- `--failure-file <失败TXT路径>` 可从失败文件读取站名和 URL。

该入口只验证指定站点和指定期数，真实读取页面、脚本、iframe、动态接口并检查锚点、字段、头值、方向窗口和同期冲突。它只打印结果，不写缓存、正式 TXT 或配置，因此单独通过它不能判定站点修复完成。

### 正式重复检测

唯一正式入口：`scripts/check_eight_consecutive_duplicates.py`

已配置站点检测：

`py -3 scripts\check_eight_consecutive_duplicates.py`

候选站参数必须同时提供：

- `--candidate-url`
- `--candidate-position`
- `--candidate-section`
- `--candidate-field`
- `--candidate-content-hint`
- `--candidate-parse-hint`

可选：`--candidate-api-url`

候选检测使用缓存最新两期进行真实方向验证，再抓取缓存期数范围。候选入口不写入 `sites.json`、缓存或正式 TXT。

候选模式返回码：

- `0`：候选模式未发现达到阈值的重复；
- `2`：配置、缓存、真实抓取或证据失败；
- `3`：连续 3 至 5 期疑似重复；
- `4`：连续 6 期或以上重复拒收。

不传任何候选参数时属于全站报告模式；该模式即使打印疑似或确定重复也固定返回 `0`，禁止凭退出码判断。必须读取报告内容并逐项处理。

`scripts/check_duplicate_heads.py` 是旧的硬编码 `150期` 至 `141期` 检查脚本，会写入非正式调试文件，禁止作为正式重复检测依据。

## 7. 近十期缓存

缓存路径：`.tmp\recent_10_cache.json`

当前静态核验结果：

- `schema`：`lottery_head_recent_10.v3`
- `latest_period`：`211期`
- `periods`：`211期` 至 `202期`
- `period_count`：10
- `sites_count`：238
- `updated_at`：`2026-07-30T18:55:47`
- 配置指纹：`6a3beda6fc8bdf29ac1376abe4b07e61951886477d9056e980dbc9b21e3a8711`
- 缓存站点数、身份集合和配置指纹与当前 `sites.json` 一致。
- 当前共有 2291 个成功值、89 个缺失期记录，涉及 39 个站点。
- 89 个缺失期均保存了失败原因；缺失不能视为成功值。
- 当前缓存中的头值、原始位置、来源 URL 和动态来源证据结构符合缓存校验代码要求。

每个缓存站点包含：

- `identity`：`section`、规范化 `url`、规范化 `position`、`parse_hint`
- `values`：期数到标准化头值
- `positions`：期数到原始候选位置
- `provenance`：来源路线、来源 URL、原文行、动态记录 ID 等
- `missing`：期数到具体失败原因

缓存无效条件包括 schema、期数窗口、站点数量、身份集合、配置指纹、成功值与缺失值覆盖、原始位置、来源证据任一不一致。缓存无效时禁止正式判重，不能把候选站判为“不重复”；但缓存无效不得阻断日常单期实时抓取，单期只可在结果确定后尝试从本轮真实记录重建/更新缓存。

当前校验器实际只要求最多 10 期，不强制必须正好 10 期；因此少于 10 期的缓存不能自动当作完整近十期基准。

## 8. 新增站点门槛

正式写入 `sites.json` 前必须满足：

1. 用户明确授权开始。
2. 真实访问原始页面或接口，不能使用截图、旧缓存、备份 JSON、历史 TXT 或人工推测。
3. 明确站名锚点、栏目、字段、方向、期号格式和数据边界。
4. `parse_hint` 必须是已注册专属解析器，不能用宽泛通用解析凑数。
5. 指定期数只接受指定期数。
6. 同站同期出现不同标准化头值，或同值但原始位置冲突，必须失败并保留原始行。
7. 候选站必须在缓存最新期或上一期中至少真实命中一期。
8. 候选站必须有带来源和原始行证据的有效近十期数据；只有一条数据、没有共同期号或抓取为空时，禁止判定“不重复”。
9. 按具体共同期号对齐，缺失期号不能跨越后组成连续重复。
10. 检查同名、同 URL、同字段、同源、同帖子 ID 和镜像关系。
11. 连续 3 至 5 期一致必须暂停并等待人工确认；连续 6 期或以上直接拒收。
12. 通过后才允许手动修改 `sites.json`。现有 BAT 和单期 CLI 只能抓当前一期，缓存失效时仍可能形成不足 10 期的缓存，不能完成正式近十期重建；在提供受控的 `sync_site_period_baseline()` 正式入口并验证每站 10 期完整性之前，新增流程不得标记完成。

当前代码没有 `topic` 字段，也没有跨域同 topic 镜像检测；因此仅依赖 `check_eight_consecutive_duplicates.py` 的通过结果，不足以完成正式新增。

## 9. 失败站点修复门槛

修复只能处理用户指定站点和指定期数。

必须真实验证：

- 主页面、script、iframe、forum、动态 API 和详情页来源是否正确；
- 标题、作者、正文是否属于同一目标记录；
- 期号是否跨行或跨文档误匹配；
- `top`/`bottom` 窗口是否正确；
- 四头组合是否完整且只缺一个头；
- 同期多个候选是否被拒绝；
- 旧文档、历史残留和相邻栏目是否被排除；
- 动态 API 404、ID 不一致、跨域响应和浏览器渲染错误是否正确失败。

修复完成必须同时满足：

- `verify_failed_sites.py` 指定期验证成功；
- 使用正式单期入口重新抓取成功；
- 成功 TXT、失败 TXT 和缓存写入正确；
- 缓存通过结构、身份、指纹和证据校验；
- 项目测试 `py -3 -m pytest -q` 通过。

静态解析样例、单个正则、单个函数或旧缓存通过，均不能报告“修复完成”。本草稿生成过程未执行网络抓取、入口、测试或正式判重，因此当前没有任何站点可据此标记为已修复。

## 10. 当前实现与统一规则冲突

以下问题必须标记为未完成，相关输出禁止作为正式结论：

- 正式重复检测同时比较标准化头值和 `positions` 原始位置；统一规则要求判重依据为标准化值，当前结果可能漏报相同头值。
- 候选站没有共同期号时，当前检测器可能输出无重复；按规则应为检测未完成，不能通过。
- 候选站只有少量有效期数时，当前检测器没有最低有效期门槛。
- 当前候选身份检查只覆盖目录名和规范化 URL，没有 topic ID、字段、镜像站和同源帖完整检查。
- 所有合法方向配置都会进入全局有序有效候选的顶部第一条/底部最后一条边界；文档收集顺序本身不等于页面视觉方向，次级来源仍必须先满足站点级来源授权和跨文档冲突门禁。
- 动态 API 返回 HTTP 200 但未命中目标记录时，当前实现仍可能进入浏览器兜底；统一规则只允许明确的 404 或空壳页面条件。
- 收集器会加入主页面、链接文档、选定 script 和最多 12 个 iframe；选择层会对任何含锚点的文档生成候选，却没有站点级来源白名单。此类次级来源命中暂不得作为正式成功，必须先建立站点级来源策略，并执行跨文档唯一性和冲突校验。
- script、iframe 请求异常在收集层可能被直接跳过，不能据此证明所有来源已完整汇总。
- 单期入口不再让缓存参与实时裁决；缓存只在实时结果确定后尝试更新，缓存写入失败不得影响成功/失败TXT。单期新建的少量期数缓存不能视为正式近十期重建。
- 多期入口与多期失败汇总脚本使用的快照文件名不一致，不能把两者输出混为正式汇总。
- 当前有效输出路径硬编码到 `C:\Users\Administrator\Desktop\每天工具\数据系列\...`，不是测试根目录 `(4)` 下的目录；未确认路径前，不得声称测试目录已生成正式输出。
- 当前缓存结构静态有效，但存在 89 个缺失期；这些期数只能作为失败证据，不能参与成功值补全或正式结论。
