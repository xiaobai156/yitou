# 杀一头_修复版

继承上级 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`。本文件只记录本子项目差异；站点配置以 `sites.json` 为准，封存数据以 `archived_sites.json` 为准。

## 范围与准确性

- 只处理“杀一头”头值，不处理生肖、波色、尾数或号码。
- 成功必须同时证明：站点身份、规范化 URL、栏目/作者锚点、字段、指定期数、`top`/`bottom` 边界、原始顺序、合法头值、来源文档和唯一性。
- 单期只接受指定期；禁止用缓存、相邻期、其它文章、其它区块或其它来源补值。
- 同期不同值、不同来源、不同区块或不同原始边界均失败；相同数据只有在同一来源、同一位置的重复展示才可去重。
- 配置身份为：站名 + 规范化 URL + 规范化方向 + `parse_hint`；`field` 参与配置指纹，`code` 不用于定位或判重。

## 方向与解析

- `top` 只取同一目标区块第一条，`bottom` 只取最后一条；窗口固定为 1。
- 必须先确认边界期数，再解析字段；边界外找到的指定期不得成功。
- 抓取层只返回原始文档；解析层只返回候选和证据；业务裁决由统一校验层完成。
- 四头数据必须是 0—4 的四个互异头，输出唯一缺失头；三头数据必须排除两个头并输出剩余两个头。
- `resource_required=True` 的资源失败时，整站失败，不能用另一份文档顶替。
- HTTP 能完整取得正确数据时优先 HTTP；必须执行 JS 或 HTTP 不足时才用 Playwright，并复用浏览器实例。

## 失败 TXT 定点重抓

```powershell
python -u scripts/retry_failed_sites.py 252期
```

- 只读取对应的 `七类数据统一归纳失败\{期数}期失败-一头.txt`，按站名、规范化 URL、规范化方向和期数匹配；重复身份只请求一次。
- 禁止转为全站运行；封存站点不抓取、不写缓存。
- 成功：追加到对应成功 TXT 的排行榜上方，更新该站该期缓存，删除对应失败记录。
- 失败：保留原失败记录；其它站点、其它期缓存和其它 TXT 不变。
- 成功 TXT 使用统一单头排行；同名不同身份或已有冲突值直接停止。
- 失败清单无法解析、成功 TXT缺少精确表头、缓存无效、目标期不在缓存窗口或文件在抓取期间变化时，三份正式文件保持原样。
- 正式提交使用项目事务锁、临时文件、原子替换和回滚；提交前停止、已回滚、回滚不完整、提交成功但备份清理失败必须分别报告。

## 正式文件

- 成功 TXT：上级 `七类数据统一归纳\{期数}期-头.txt`。
- 失败 TXT：上级 `七类数据统一归纳失败\{期数}期失败-一头.txt`。
- 近 10 期缓存：本项目 `.tmp\recent_10_cache.json`；缓存按完整配置身份绑定。
- `.tmp`、凭据、调试产物不得提交或推送。

## 开发与同步

- 遵循 Ponytail full：先复用现有实现，保持最小改动，不增加无明确收益的抽象。
- 修改后运行受影响的离线检查、Python 编译和必要的指定期真实流程；没有真实验证不得声称完成。
- 当前项目 `origin` 为 `https://github.com/xiaobai156/yitou`。验证通过后，只提交本次修改，保留已有未提交改动，并自动推送；不推送凭据、临时文件或其它项目。
